name: host-tests
on:
  push:
    branches: [main]
  pull_request:

jobs:
  host-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build + run oracle test suite (clang, ASan/UBSan)
        run: bash host/build.sh
